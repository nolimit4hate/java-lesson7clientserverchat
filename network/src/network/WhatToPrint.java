package network;

/*
 * key of what to print logic
 */

public enum WhatToPrint {
    CLIENT_CONNECTED, CLIENT_DISCONNECTED, MESSAGE, SERVERONLY, ENTRYERROR, PORTERROR, IO_EXCEPTION,
    NOT_RESPOND_REQUEST, CONNECTION_EX;
}
